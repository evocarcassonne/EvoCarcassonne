<UserControl x:Class="EvoCarcassonne.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:modules="clr-namespace:EvoCarcassonne.Views.Modules"
             xmlns:viewModels="clr-namespace:EvoCarcassonne.ViewModels"
             mc:Ignorable="d"
             MinHeight="450" MinWidth="800">
    <Grid Margin="5">
        <!-- Current player module -->
        <modules:PlayerListModule Panel.ZIndex="1" VerticalAlignment="Top" HorizontalAlignment="Left"></modules:PlayerListModule>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Game board module -->
            <modules:BoardModule Grid.Row="0" />

            <!-- Tile control module -->
            <modules:BottomControlModule Grid.Row="1" />
        </Grid>
    </Grid>
</UserControl>